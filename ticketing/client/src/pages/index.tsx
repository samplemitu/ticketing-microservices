// This file is kept for reference but the main page is now TicketsListPage
// Redirecting to the tickets list

import { Navigate } from "react-router-dom";

const Index = () => {
  return <Navigate to="/" replace />;
};

export default Index;
